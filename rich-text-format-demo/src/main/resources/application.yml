spring:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    open-in-view: false

  datasource:
    url: jdbc:mysql://localhost:3306/personal?useSSL=false&serverTimezone=GMT%2b8&characterEncoding=UTF-8
    username: root
    password: 20171109
    driver-class-name: com.mysql.cj.jdbc.Driver

  thymeleaf:
    prefix: classpath:/templates/
    cache: false
    suffix: .html
    encoding: utf-8
    servlet:
      content-type: text/html
    mode: HTML5

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
# vsftp 配置文件
ftp:
  ip: localhost
  port: 21
  username: ftpuser
  password: fptuser123
  romote-path: personal

server:
  port: 8080